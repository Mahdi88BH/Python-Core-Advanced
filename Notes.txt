1. Function Call
    - In computer programming, a function call (or function invocation) is the act of directing the computer to execute a specific, pre-defined block of code.
    - When you "call" a function, the program temporarily pauses its current flow, jumps to the function's definition, runs the instructions inside, and then returns to where it left off.